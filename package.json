{
  "name": "buscohuella",
  "version": "0.1.0",
  "private": true,
  "description": "Plataforma digital colaborativa para la búsqueda y protección de mascotas.",
  "license": "UNLICENSED",
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=22.12.0 <23",
    "pnpm": ">=10.32.1 <11"
  },
  "scripts": {
    "dev": "pnpm --filter @buscohuella/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  }
}
